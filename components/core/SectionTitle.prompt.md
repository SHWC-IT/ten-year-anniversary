Cinzel caps heading with an optional gold Great Vibes script flourish; the system's primary section header.

```jsx
<SectionTitle script="Harvest Celebration.">Gathering of Nations</SectionTitle>
<SectionTitle align="center" as="h1">Sow into the next decade</SectionTitle>
```

Keep the `script` accent to one or two words. Use `as="h1"` for page heroes.
